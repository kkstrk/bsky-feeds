{
  "name": "bsky-feeds",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "publishFeed": "node ./scripts/publishFeedGen.mjs",
    "deleteFeed": "node ./scripts/deleteFeedGen.mjs",
    "deploy": "wrangler deploy",
    "start": "wrangler dev --remote",
    "debug": "wrangler tail --format=pretty",
    "lint": "eslint --ext .js src"
  },
  "devDependencies": {
    "eslint": "^8.46.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@atproto/api": "^0.4.3",
    "dotenv": "^16.3.1"
  }
}
